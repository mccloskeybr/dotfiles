# ls
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# mv
alias mv='mv -i'

# rm
alias rm='rm -i'

# cp
alias cp='cp -i'

# grep
alias grep='grep --color=auto'

# fig
alias h=hg

# plugins
alias history=histdb

